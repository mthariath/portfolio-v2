import React from "react";
import { animated, Keyframes, config } from "react-spring";

const SlideOnMount = ({ children }) => {
  const css = { adf: "df" };
  return <animated.div>{children}</animated.div>;
};
